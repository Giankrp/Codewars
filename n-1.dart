/// Devuelve la última silla ocupada en una secuencia de 1..n asumiendo
/// que se elimina una silla en cada paso hasta quedar una.
/// Aquí, por el planteamiento del reto, el resultado es `n-1`.
int lastChair(int n) {
  return n - 1;
}
